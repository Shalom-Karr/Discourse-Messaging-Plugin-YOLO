name: Plugin Tests

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    uses: discourse/.github/.github/workflows/plugin-tests.yml@main
    with:
      repository: discourse/discourse
